{
    "swagger": "2.0",
    "info": {
        "title": "Jasmin",
        "description": "Project Jasmin API documentation",
        "version": "1.0.0"
    },
    "host": "localhost:8080",
    "basePath": "/api",
    "schemes": ["http"],
    "produces": ["application/json"],
    
    "paths": {
        "/Breeds": {
            "get": {
                "tags": ["Breeds"],
                "summary": "Breeds from MongoDB",
                "responses": {
                    "200": {
                        "description": "Breeds displayed successfully!",
                        "schema": {
                            "$ref": "#/definitions/Breeds"
                        }
                    }
                }
            }                
        }
    },
    "definition": {
        "Breeds": {
            "required": ["Breed"],
            "properties": {
                "Breed":{
                    "type": "string",
                    "uniqueItems": true
                }
            }
        }
    },
    "Breeds": {
        "type": "array",
        "$ref": "#/definition/Breeds"
    }
}

